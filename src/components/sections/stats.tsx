"use client";

// Stats is now integrated into the Services section.
// This file is kept as an empty placeholder to avoid breaking imports
// (the page.tsx no longer imports it).

export function Stats() {
  return null;
}
